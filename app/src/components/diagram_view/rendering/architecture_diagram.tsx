import { Diagram, type DiagramProps } from './diagram'

/** Architecture mapping onto shared diagram primitives. */
export function ArchitectureDiagram(props: DiagramProps) {
    return <Diagram {...props} />
}
