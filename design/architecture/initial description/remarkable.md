---
internalId: 2beca491-5218-4ad1-817e-228978ff4383
---

## Remarkable integration

- allow for auto import of images from the Remarkable device.

  - use SSH to connect to the device
  - list files on device, allow user to select what to import
  - keep track of last import date of files, so we can show which files have been changed since last import
  - git files → ask where to put → in new feature or existing
  - commit to GitHub

- when agent is available, include command to convert to text.

- images are stored in the same folder as the card-file, so relative referencing allowed.

  - This means that the images need to be copied over together with feature for release.

