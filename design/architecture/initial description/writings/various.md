---
internalId: 96cf16d6-0cf3-45d0-b5d2-3bfb27d5b2eb
---

```markdown



- Markdown styles need to be
  editable
  -> predefined names are
     in list, includes 'custom'
  When user changes setting when a
  predefined style is selected, it switches
  to 'custom'

- Group config types -> styling, project


```


# done
- When switching from list to cards
  view and back -> restore open
  cards. Use a service to track
  open cards on trigger events
  -> includes actions
- Add folder + add file button
  in tree view + in context menu
  -> regular md files
  -> ex: for architecture
  -> create regular folder and md files

- Make certain undo data
  is linked to the card so tab
  switches preserve the undo data
  correctly.
- Use "_" as default allow configuration. When
  changing -> old files need to be
  renamed -> show dialog with
  warning
- show icon in tabs when in text-view to show what type it is: action, card, regular md file