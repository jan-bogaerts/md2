# Diff View

* Action logs should store commit numbers (perhaps generic text output of the "commit-type action")

* When an action log has this commit field, a diff view can be shown

  * When clicking on a line in the diff → open VS Code

* The diff should be generated by a custom, configurable command line statement